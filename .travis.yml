sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start Creating an image with dockerfile"
  - docker build -t dudckd/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true docker dudckd/docker-react npm run test -- --coverage

after_success:
  - echo "Test success"